class Solution:
    def search(self, nums: List[int], target: int) -> int:
        for i in range(len(nums)):
            x=nums.index
            if target in nums:
                return x(target) 
            else:
                return -1

        