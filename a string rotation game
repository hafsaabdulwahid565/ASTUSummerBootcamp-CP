def count_blocks(s):
    blocks = 1
    for i in range(1, len(s)):
        if s[i] != s[i-1]:
            blocks += 1
    return blocks
n=int(input())
for i in range(n):
    x=int(input())
    s=input().strip()
    z=0
    max_blocks = count_blocks(s)

    for j in range(len(s)):
         y=s[j:]+s[:j]
         max_blocks = max(max_blocks, count_blocks(y))
    print (max_blocks)
    

